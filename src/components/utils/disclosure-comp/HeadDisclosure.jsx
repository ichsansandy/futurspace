import React from 'react';

export default function HeadDisclosure({ title }) {
  return (
    <span className="text-sm font-bold web:text-2xl text-secondary opacity-90">
      {title}
    </span>
  );
}
